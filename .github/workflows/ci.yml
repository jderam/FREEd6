name: CI

on:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.14"
      - name: Install dependencies
        run: uv sync --extra=dev --frozen
      - name: Version Check
        run: |
          git fetch origin main --depth=1
          CURRENT=$(grep -m1 '__version__' src/free_d6/__init__.py | cut -d'"' -f2)
          MAIN=$(git show origin/main:src/free_d6/__init__.py | grep -m1 '__version__' | cut -d'"' -f2)
          python3 -c "
          current = tuple(int(x) for x in '$CURRENT'.split('.'))
          main = tuple(int(x) for x in '$MAIN'.split('.'))
          assert current > main, f'Version $CURRENT must be higher than main ($MAIN)'
          "
      - name: Changelog Check
        run: |
          VERSION=$(grep -m1 '__version__' src/free_d6/__init__.py | cut -d'"' -f2)
          grep -q "## \[$VERSION\]" changelog.md || (echo "No changelog entry for v$VERSION" && exit 1)
      - name: Lint Check
        run: uv run ruff check .
      - name: Format Check
        run: uv run ruff format --check .

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: uv sync --extra=dev --frozen
      - name: Run tests
        run: uv run pytest tests/
